# 2.5.7 - Retorno QA do seek da timeline

Gerado em: 2026-07-17  
Sessao/contexto: editor-timeline-seek-audio-qa-1  
Tipo de registro: retorno-qa

## Card

Arquivo do card: [workground](.)  
Resumo:

- Corrigir a nova regressão observada após a primeira correção do scrub da timeline.

## Pedido Do Usuario

Pedido literal:

> Depois de ter mudado a régua pra qualquer ponto da timeline. A imagem pode ficar indo e voltando em uma sequência de frames repetidos, o áudio leva 5s pra poder sair do lugar.

Esclarecimento posterior:

> O vídeo que to me referindo de travamento na verdade é o vídeo da webcam.

Artefatos fornecidos:

- Projeto: `C:\Users\andre\OneDrive\Documentos\RecordSaaS\TesteFailRegua`
- Vídeo do comportamento: `C:\Users\andre\Videos\Gravações de Tela\Gravação de Tela 2026-07-17 154950.mp4`

## Interpretacao E Inferencias

- O projeto tem vídeo de 191,82s, áudio de microfone de 191,54s e áudio do sistema de 151,04s; os arquivos abrem normalmente no FFmpeg.
- O atraso é compatível com seeks de áudio AAC acumulados durante o arraste, não com arquivo exportado corrompido.
- Durante scrubbing, os áudios não devem receber um novo `currentTime` a cada movimento; o sync deve acontecer uma vez no final.
- O travamento visual está no elemento de webcam; o loop vinha da ressincronização repetida do `webcamVideo.currentTime` no ciclo de renderização.

## Implementacao Ou Alteracao

- O que foi feito: o scrub da régua agora coalesce movimentos com `requestAnimationFrame` e aplica a posição final no mouse up.
- O que foi feito: o Editor compartilha o estado de scrubbing entre Timeline e Preview; durante o arraste, os áudios apenas pausam, sem novos seeks AAC.
- O que foi feito: o estado de scrubbing permanece ativo até o `seeked` final, com fallback de segurança; reprodução antiga não pode reativar áudio/vídeo durante a busca.
- O que foi feito: ao terminar o arraste, o Preview sincroniza vídeo, webcam e áudios uma única vez; o canvas continua redesenhando no `seeked`.
- O que foi feito: o webcam não é reposicionado durante o scrub e o resync durante playback é limitado e ignorado enquanto o elemento está em `seeking`.
- Onde foi feito: [Preview.tsx](../../../src/components/editor/Preview.tsx), [Timeline.tsx](../../../src/components/editor/Timeline.tsx), [useTimelineInteraction.ts](../../../src/hooks/useTimelineInteraction.ts), [EditorPage.tsx](../../../src/pages/EditorPage.tsx), [AGENTS.MD](../../../AGENTS.MD).
- O que não será feito: nenhuma alteração em captura, FFmpeg, exportação ou arquivos do projeto do usuário.

## Commits Relacionados Ao Card

Repositorio: `E:\GitHub\recordsaas-solutions\recordsaas`

- Branch: `main`
- Base/destino: `main`
- Commits: alteração QA ainda não commitada; os commits `c61890c` e `7a1e2b1` permanecem como antecedentes de seek.
- Push/PR: not pushed.

## Validacao

- Validacoes feitas: inspeção do vídeo anexado por frames; FFmpeg nos quatro artefatos de mídia; leitura do `project.rsproj`; inspeção do fluxo atual do renderer; TypeScript; ESLint; Prettier; `npm run build`; SCC após a correção final.
- Resultado dos artefatos: vídeo 191,82s/60fps; áudio de microfone 191,54s; áudio do sistema 151,04s; todos decodificaram sem erro no FFmpeg.
- Resultado SCC: snapshot atual de 4 arquivos, complexidade 528, ULOC 2030, DRYness 0,64; sem baseline equivalente da alteração QA.
- Validacoes pendentes: teste funcional manual do Editor com o projeto `TesteFailRegua`.

## Riscos E Pendencias

- O teste funcional ainda precisa confirmar que vídeo e áudio retomam juntos após o `seeked`, sem atraso acumulado.
- A gravação do usuário mostra o sintoma, mas não permite confirmar a latência interna dos elementos HTMLMediaElement sem executar o build no Editor.

## Proximo Teste Esperado

- Abrir `TesteFailRegua`, arrastar a régua rapidamente por vários pontos, soltar e iniciar/reiniciar o play; confirmar frame estável e áudio no mesmo timestamp.
