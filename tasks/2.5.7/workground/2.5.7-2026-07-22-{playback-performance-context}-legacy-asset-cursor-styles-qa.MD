# 2.5.7 - Tela branca ao abrir projeto legado

Gerado em: 2026-07-22
Sessao/contexto: playback-performance-context
Tipo de registro: retorno-qa

## Card

Arquivo do card: [workground 2.5.7](.)
Resumo:

- Impedir crash do Preview quando asset legado nao possui estilos de cursor locais.

## Pedido Do Usuario

Pedido literal:

> Quando abre, fica na tela branca e constante.

Classificacao:

- Pedido explicito: corrigir tela branca ao abrir projeto.
- Restricoes explicitas: usar log anexado como evidencia.

## Interpretacao E Inferencias

- Inferencia operacional: erro nao e de Vite, FFmpeg ou protocolo de media.
- Motivo: log confirma `Uncaught TypeError: Cannot read properties of undefined (reading 'clickRippleEffect')` no `Preview` apos projeto ser entregue.
- Risco da inferencia: projeto legado pode ter `timeline.cursorStyles` ausente; spread da timeline nao pode sobrescrever fallback do projeto por `undefined`.

## Implementacao Ou Alteracao

- [renderer](../../../src/lib/renderer.ts) agora usa `state.cursorStyles` do contexto pai quando timeline interna nao possui `cursorStyles`.
- Evita `Uncaught TypeError: Cannot read properties of undefined (reading 'clickRippleEffect')` e queda inteira do componente `Preview`.
- O que nao foi feito: abertura visual do projeto legado no Electron.

## Commits Relacionados Ao Card

Repositorio: recordsaas

- Branch: `main`
- Base/destino: `origin/main`
- Commits anteriores relevantes:
  - `8f287b7` - `fix(editor): restore nested asset editing` - 2026-07-22 - adicionou timeline interna de asset.
- Commits desta alteracao:
  - `7ea2164` - `fix(editor): handle legacy asset cursor styles` - 2026-07-22 - fallback de cursor em composicao aninhada.
- Push/PR: nao enviado.

## Validacao

- Validacoes feitas: leitura do log e rastreio de [renderer](../../../src/lib/renderer.ts) com [projectSlice](../../../src/store/slices/projectSlice.ts); `npx tsc --noEmit --pretty false`; ESLint focal; `npm run build`; `git diff --check`; SCC focal.
- Validacoes nao feitas: abertura visual do projeto legado.
- Motivo das validacoes nao feitas: requer importacao pelo Electron com os arquivos do usuario.

## Riscos E Pendencias

- Confirmar abertura de `TesteFailRegua/project.rsproj` sem tela branca.

## Proximo Teste Esperado

- Importar projeto legado contendo asset editado sem `timeline.cursorStyles`; preview deve abrir e renderizar normalmente.
