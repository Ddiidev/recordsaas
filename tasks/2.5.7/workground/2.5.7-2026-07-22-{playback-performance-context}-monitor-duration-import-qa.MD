# 2.5.7 - Duracao de monitor apos importar projeto

Gerado em: 2026-07-22
Sessao/contexto: playback-performance-context
Tipo de registro: retorno-qa

## Card

Arquivo do card: [workground 2.5.7](.)
Resumo:

- Preservar duracao de composicao de monitor no import.

## Pedido Do Usuario

Pedido literal:

> Elemento na timeline ficou minimo e nao da para redimensionar; antes de salvar durava cerca de 3s.

Classificacao:

- Pedido explicito: restaurar duracao persistida de monitor.
- Restricoes explicitas: regiao deve continuar redimensionavel.

## Interpretacao E Inferencias

- Inferencia operacional: import estava usando `monitor.duration` bruto para limitar regiao, embora composicao editada use `monitor.timelineDuration`.
- Motivo: video salvo pode iniciar com metadado de duracao `0`; normalizacao reduzia regiao para minimo de `0.1s`.
- Risco da inferencia: manter `timelineDuration` sem metadado permite abrir projeto antes de Chromium carregar video; depois metadado real continua sendo normalizado.

## Implementacao Ou Alteracao

- [projectSlice](../../../src/store/slices/projectSlice.ts) preserva `timelineStart` e `timelineDuration` salvos quando video ainda nao tem metadado de duracao.
- Regiao de monitor agora limita por `timelineDuration` da composicao editada; nao por `monitor.duration` transitorio.
- O que nao foi feito: importacao visual no Electron.

## Commits Relacionados Ao Card

Repositorio: recordsaas

- Branch: `main`
- Base/destino: `origin/main`
- Commits anteriores relevantes:
  - `8f287b7` - `fix(editor): restore nested asset editing` - 2026-07-22 - persistencia de timeline de asset.
- Commits desta alteracao:
  - `dd7be04` - `fix(editor): preserve monitor composition duration` - 2026-07-22 - impede reducao para 0.1s no import.
- Push/PR: nao enviado.

## Validacao

- Validacoes feitas: leitura de [projectSlice](../../../src/store/slices/projectSlice.ts) e comparacao com captura QA; `npx tsc --noEmit --pretty false`; ESLint focal; `npm run build`; `git diff --check`; SCC focal.
- Validacoes nao feitas: importacao visual do projeto.
- Motivo das validacoes nao feitas: requer arquivo salvo real do usuario.

## Riscos E Pendencias

- Confirmar regiao de cerca de 3s ao reabrir projeto salvo.

## Proximo Teste Esperado

- Salvar projeto com monitor de 3s, reabrir e redimensionar ambas alcas na timeline.
