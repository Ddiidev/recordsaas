# 2.5.7 - Blur em edicao de imagem

Gerado em: 2026-07-22
Sessao/contexto: playback-performance-context
Tipo de registro: retorno-qa

## Card

Arquivo do card: [workground 2.5.7](.)
Resumo:

- Permitir adicionar Blur com uma regiao selecionada em edicao de imagem.

## Pedido Do Usuario

Pedido literal:

> Quando abro a edicao de imagem nao consigo adicionar elementos visuais a timeline, como blur.

Classificacao:

- Pedido explicito: liberar Blur em timeline de imagem.
- Restricoes explicitas: manter elementos visuais disponiveis durante edicao de imagem.

## Interpretacao E Inferencias

- Inferencia operacional: `addBlurRegion` ja aceita a timeline de imagem; apenas o botao era desativado quando havia selecao.
- Motivo: a acao usa a lane da regiao selecionada como destino, sem conflito.
- Risco da inferencia: nenhum contrato de timeline muda; altera somente disponibilidade da acao.

## Implementacao Ou Alteracao

- [PreviewControls](../../../src/components/editor/PreviewControls.tsx) nao desativa mais Blur quando existe regiao selecionada.
- `addBlurRegion` usa a lane selecionada como destino; por isso a selecao nao e bloqueio valido.
- O que nao foi feito: validacao visual interativa no Electron.

## Commits Relacionados Ao Card

Repositorio: recordsaas

- Branch: `main`
- Base/destino: `origin/main`
- Commits anteriores relevantes:
  - `8f287b7` - `fix(editor): restore nested asset editing` - 2026-07-22 - fluxo de timeline temporaria.
- Commits desta alteracao:
  - `097650b` - `fix(editor): allow blur in image asset editing` - 2026-07-22 - libera acao Blur com selecao ativa.
- Push/PR: nao enviado.

## Validacao

- Validacoes feitas: inspecao de [PreviewControls](../../../src/components/editor/PreviewControls.tsx) e [timelineSlice](../../../src/store/slices/timelineSlice.ts); `npx tsc --noEmit --pretty false`; ESLint focal; `npm run build`; `git diff --check`; SCC focal.
- Validacoes nao feitas: validacao visual Electron.
- Motivo das validacoes nao feitas: requer projeto aberto pelo usuario e asset de imagem real.

## Riscos E Pendencias

- Confirmar que Blur cria regiao na lane selecionada da timeline de imagem.

## Proximo Teste Esperado

- Abrir asset de imagem, manter monitor selecionado, clicar Blur e ajustar o overlay no preview.
