# 2.5.7 - Centro do viewport como ancora de zoom

Gerado em: 2026-08-03
Sessao/contexto: 019fc892-context
Tipo de registro: retorno-qa

## Card

Registro anterior: [2.5.7 - Ancora de scroll no zoom](2.5.7-2026-08-03-{019fc892-context}-timeline-zoom-scroll-anchor.MD)

Resumo:

- Corrigir a ancora de zoom de borda esquerda para centro temporal do viewport.

## Pedido Do Usuario

Pedido literal:

> Nao funcionou. O elemento selecionado deixa de ficar visivel apos o zoom e preciso encontrá-lo rolando horizontalmente. Preciso resolver isso.

Classificacao:

- Pedido explicito: manter o contexto temporal que estava visivel antes do zoom.
- Restricoes explicitas: nao obrigar scroll horizontal manual para reencontrar o elemento/contexto.

## Interpretacao E Inferencias

- Inferencia operacional: usar o centro temporal do viewport como ancora, nao a borda esquerda.
- Motivo: nas evidencias, a imagem inicial mostra o playhead/contexto perto de `08:40`; a ancora anterior manteve `00:00` e expulsou essa area. A imagem apos scroll manual volta para aproximadamente `08:40`.
- Risco da inferencia: no inicio/fim da timeline, a ancora e limitada pelo scroll maximo/minimo fisico.

## Implementacao Ou Alteracao

- O que foi feito: calcula `visibleCenterTime` antes de mudar pixels por segundo e reposiciona `scrollLeft` para manter esse tempo no centro da area visivel.
- Por que foi feito: manter a borda esquerda fixa preservava `00:00`, nao o contexto que o usuario estava editando no meio da timeline.
- Onde foi feito: [Timeline.tsx](../../../src/components/editor/Timeline.tsx).
- O que nao foi feito: alterar regiao, playhead, seek ou a escala de zoom.

## Commits Relacionados Ao Card

Repositorio: `E:\GitHub\recordsaas-solutions\recordsaas`

- Branch: `main`
- Base/destino: `origin/main`
- Commits anteriores relacionados:
  - `88d6983` - `fix(editor): preserve playback state on ruler seek` - 2026-07-21 - preservacao de contexto na timeline.
  - `395af92` - `fix(editor): remove preview seek hot paths` - 2026-07-21 - interacao recente da timeline.
- Novo commit: nenhum; alteracao ainda nao foi commitada.
- Push/PR: not pushed.

## Validacao

- Validacoes feitas: `npx tsc --noEmit --pretty false`, `npm run build`, Prettier do codigo/WIP, `git diff --check` e SCC de `Timeline.tsx`.
- Resultado: TypeScript e build de producao passaram. SCC sem baseline: 806 ULOC, complexidade 206 e DRYness 0,64; classificacao neutra, sem refactor adicional.
- Validacoes nao feitas: teste manual Electron.
- Motivo das validacoes nao feitas: usuario validara a interacao na timeline.

## Riscos E Pendencias

- Validar zoom in/out com playhead no meio, no inicio e no fim da timeline.

## Proximo Teste Esperado

- Deixar o playhead em `08:40`, aplicar `2,5x -> 24x` e confirmar que a regiao em torno de `08:40` continua visivel, sem scroll manual.
