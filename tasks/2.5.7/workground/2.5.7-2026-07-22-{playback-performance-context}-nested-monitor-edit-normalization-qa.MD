# 2.5.7 - Normalizacao de monitor Edit em timeline temporaria

Gerado em: 2026-07-22
Sessao/contexto: playback-performance-context
Tipo de registro: retorno-qa

## Card

Arquivo do card: [WIP anterior](2.5.7-2026-07-22-{playback-performance-context}-nested-monitor-add-edit-clone-qa.MD)
Resumo:

- Corrigir destaque e referencia de regioes legadas dentro de video Edit.

## Pedido Do Usuario

Pedido literal:

> continua, veja na imagem, até tracei uma seta para mostrar onde ele está destacando.

Classificacao:

- Pedido explicito: regiao e borda precisam apontar card inferior com badge `Edit`.
- Restricoes explicitas: contexto e timeline de video em edicao, nao main.

## Interpretacao E Inferencias

- Inferencia operacional: projeto salvo confirmou timeline interna do video Edit com `monitorId` da imagem original, apesar de clone existir.
- Motivo: correcao de adicao nao modifica instancia ja existente; abrir timeline tambem precisava normalizar legado.
- Risco da inferencia: resolucao por `originalName`+`path` usa clone existente unico; fluxo evita criar duplicados.

## Implementacao Ou Alteracao

- O que foi feito: novo [floating-monitor.ts](../../../src/lib/floating-monitor.ts) centraliza busca de clone e normalizacao de regioes.
- O que foi feito: [projectSlice.ts](../../../src/store/slices/projectSlice.ts) normaliza composicao ao abrir edicao; [timelineSlice.ts](../../../src/store/slices/timelineSlice.ts) usa mesma regra ao adicionar; [MediaAssetsPanel.tsx](../../../src/components/editor/sidepanel/MediaAssetsPanel.tsx) resolve destaque imediatamente.
- Por que foi feito: tornar referencia persistida, nova adicao e feedback visual consistentes.
- O que nao foi feito: main timeline nao recebe normalizacao automatica.

## Commits Relacionados Ao Card

Repositorio: `recordsaas`

- Branch: `main`
- Base/destino: `origin/main`
- Commits:
  - Pendente - normalizacao de regioes em timeline temporaria.
- Push/PR: nao enviado.

## Validacao

- Validacoes feitas: inspecao estruturada do projeto `TesteFailRegua` confirmou regiao interna em `floating-monitor-1784722184080` (original), enquanto clone `floating-monitor-1784722208175` existe.
- Validacoes nao feitas: reproducao manual no Electron.
- Motivo das validacoes nao feitas: requer reabrir video Edit apos atualizacao.

## Riscos E Pendencias

- Confirmar que abrir video Edit corrige regiao existente e que borda muda para card Edit sem recriar monitor.

## Proximo Teste Esperado

- Fechar/reabrir `kid-goku ... Edit`; regiao interna e borda devem apontar imagem `Edit` inferior.
