# 2.5.7 - Barra de efeitos na edicao de imagem

Gerado em: 2026-07-22
Sessao/contexto: playback-performance-context
Tipo de registro: retorno-qa

## Card

Arquivo do card: [WIP anterior](2.5.7-2026-07-22-{playback-performance-context}-nested-asset-clone-reference-qa.MD)
Resumo:

- Reexibir barra de acoes acima da timeline durante edicao de imagem.

## Pedido Do Usuario

Pedido literal:

> aparece os elementos acima da timeline para adicionar efeitos na imagem, como blur, por exemplo. Havia falado isso antes, porém não foi ajustado isso.

Classificacao:

- Pedido explicito: exibir acoes de timeline, incluindo Blur, em edicao de imagem.
- Restricoes explicitas: manter controles ocultos apenas em preview fullscreen.

## Interpretacao E Inferencias

- Inferencia operacional: usar mesma barra `PreviewControls` das outras timelines.
- Motivo: `Blur` ja aceita timeline de imagem; condicao de renderizacao inteira impedia acesso.
- Risco da inferencia: acoes temporalmente incompativeis continuam seguindo suas validacoes atuais; este ajuste nao altera semantica de regioes.

## Implementacao Ou Alteracao

- O que foi feito: [EditorPage.tsx](../../../src/pages/EditorPage.tsx) nao oculta mais `PreviewControls` quando asset ativo e imagem.
- Por que foi feito: condicao `editingAssetIsImage` escondia barra inteira, inclusive `Blur`.
- O que nao foi feito: nenhum fluxo de fullscreen foi alterado.

## Commits Relacionados Ao Card

Repositorio: `recordsaas`

- Branch: `main`
- Base/destino: `origin/main`
- Commits:
  - Pendente - exibicao da barra na edicao de imagem.
- Push/PR: nao enviado.

## Validacao

- Validacoes feitas: rastreio entre `EditorPage` e `PreviewControls`.
- Validacoes nao feitas: reproducao manual no Electron.
- Motivo das validacoes nao feitas: depende de abrir asset de imagem.

## Riscos E Pendencias

- Confirmar visibilidade de Blur e criacao da regiao na timeline de imagem.

## Proximo Teste Esperado

- Abrir imagem `Edit`; barra deve aparecer acima da timeline e `Blur` deve adicionar regiao.
