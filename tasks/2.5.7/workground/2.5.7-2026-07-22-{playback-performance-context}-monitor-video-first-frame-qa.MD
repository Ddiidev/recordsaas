# 2.5.7 - Primeiro frame de video monitor

Gerado em: 2026-07-22
Sessao/contexto: playback-performance-context
Tipo de registro: retorno-qa

## Card

Arquivo do card: [WIP anterior](2.5.7-2026-07-22-{playback-performance-context}-nested-asset-clone-legacy-reference-qa.MD)
Resumo:

- Renderizar video de monitor assim que metadado estiver pronto.

## Pedido Do Usuario

Pedido literal:

> O vídeo monitor só é exibido depois que eu reposiciono ele de novo, do contrário ele ficou preto, porém o monitor que estava dentro dele foi exibido corretamente!

Classificacao:

- Pedido explicito: video monitor deve renderizar sem reposicionamento manual.
- Restricoes explicitas: manter monitor aninhado funcional.

## Interpretacao E Inferencias

- Inferencia operacional: metadado do video chega depois do primeiro desenho do canvas; fonte nao participa do draw enquanto `videoWidth` e zero.
- Motivo: reposicionar muda estado e causa redraw posterior, quando metadado ja existe.
- Risco da inferencia: sincronizar em `loadedmetadata` usa fila de seek existente para evitar seeks concorrentes.

## Implementacao Ou Alteracao

- O que foi feito: [Preview.tsx](../../../src/components/editor/Preview.tsx) sincroniza monitor ao tempo atual e agenda redraw ao receber `loadedmetadata`.
- Por que foi feito: canvas passa a receber fonte com dimensoes validas sem depender de interacao do usuario.
- O que nao foi feito: nao foram criadas fontes/canvases novos no hot path.

## Commits Relacionados Ao Card

Repositorio: `recordsaas`

- Branch: `main`
- Base/destino: `origin/main`
- Commits:
  - Pendente - sincronizacao de primeiro frame de monitor.
- Push/PR: nao enviado.

## Validacao

- Validacoes feitas: rastreio de `floatingMonitorVideoRefs`, filtro de dimensoes do canvas, fila de seek e callback de metadado.
- Validacoes nao feitas: reproducao manual no Electron.
- Motivo das validacoes nao feitas: requer importacao e preview do video monitor.

## Riscos E Pendencias

- Confirmar preview pausado e em playback para monitor com composicao aninhada.

## Proximo Teste Esperado

- Abrir projeto e navegar para tempo com video monitor; primeiro frame deve aparecer sem mover/regiao.
